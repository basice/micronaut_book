pluginManagement {
    repositories {
        maven { url "https://a.repo-k1.utv/artifactory/maven2-remote" }
        maven { url "https://a.repo-k1.utv/artifactory/gradle-remote" }
        maven { url "https://a.repo-k1.utv/artifactory/jfrog-tools-remote" }
    }
}

rootProject.name = "demo"