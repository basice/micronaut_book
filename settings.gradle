pluginManagement {
    repositories {
        maven { url "https://a.repo-k1/artifactory/maven2-remote" }
        maven { url "https://a.repo-k1/artifactory/gradle-remote" }
	}
}

rootProject.name="demo"


